# VM examples

Sample Hack VM bytecode programs for the runtime in this repository.

Run an example from the repository root:

```bash
python -m vm.run examples/vm/simple_add.vm -d -s
```

or

```bash
python -m vm.run examples/vm/multi_file/ -d -s
```
