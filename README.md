
## Install as a CLI library

From the project root:

```bash
uv pip install -e .
```

After that, the `jacke` command is available from any directory:

```bash
jacke /path/to/jack/files
```

You can see compiler docs [here](./compiler/README.md)