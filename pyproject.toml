[project]
name = "jacke"
description = "Jack compilation and run"
readme = "README.md"
requires-python = ">=3.14"
version = "0.01"
dependencies = [
    "mypy>=1.19.1",
    "pytest>=9.0.2",
    "ruff>=0.15.4",
]
